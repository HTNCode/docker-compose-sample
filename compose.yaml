version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
      secrets:
        - hf_token
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      - FLASK_APP=backend/main.py

secrets:
  hf_token:
    file: ./backend/.env
